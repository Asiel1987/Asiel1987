# Development environment
# Vite exposes vars prefixed with VITE_ to client-side code via import.meta.env
VITE_API_BASE=
VITE_APP_ENV=development
VITE_STRIPE_PK=pk_test_replace_with_your_test_key
# Social sign-in (leave empty to run in demo/bypass mode)
VITE_GOOGLE_CLIENT_ID=
VITE_APPLE_CLIENT_ID=
